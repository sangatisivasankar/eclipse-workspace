package com.sapient.endur.service;

public class Source {
	public String handleException(Activity a)   {
		/*
			  if operand1 or operand 2 or operator is null, throw NullPointerException
			   with a message, "operand1 or operand 2 or operator cannot be null".
			   catch the above exception and return the message.

			   if operator is other + or -, throw OperatorException with a message
			   "operator should be either + or -" .Catch the exception and return the message.

				Note: OperatorException is custom checked exception.
				
			   if operand1, operand 2 and operator are valid, return message "valid data" else
			   return "invalid data"

		 */
		//TO DO
		return null;
		
	}

	public String doOperation(Activity a)  {
		/*
			  if operator is + then return concatenation of operand1 and operand2
			  if operator is - then  return operand2 removing all the characters that
			  are in operand1. If operand1 is "hello" and operand2 is "welcome",  result is wcm.
			  
			  If invalid operator, return null
			  
		 */
		 //TO DO
		return null;
		
	}
}











